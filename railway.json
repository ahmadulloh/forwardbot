{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --worker-class=gthread --threads=4 --bind=0.0.0.0:${PORT:-8080} app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
